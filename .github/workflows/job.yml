name: Print Date and Time

on:
  push:
    branches: ["*"]
  pull_request:
    branches: ["*"]

jobs:
  print-date:
    runs-on: ubuntu-latest
    steps:
      - name: Show current date and time
        run: date
